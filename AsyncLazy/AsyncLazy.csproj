<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netcoreapp2.0</TargetFramework>
    <Authors>Péter Eke</Authors>
    <Company />
    <Description>Lazy execution for your C# adventures! Contains classes like AsyncLazy and Once.</Description>
    <RepositoryUrl>https://github.com/peterekepeter/async-lazy</RepositoryUrl>
    <PackageReleaseNotes>Since Async lazy will most probably be used in multithreaded contexts, now there is a semaphore in the implementation so that the resource is only initialized once.</PackageReleaseNotes>
    <Version>1.2.0</Version>
    <AssemblyVersion>1.2.0.0</AssemblyVersion>
    <FileVersion>1.2.0.0</FileVersion>
  </PropertyGroup>

</Project>
